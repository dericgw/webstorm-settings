import React from 'react';

#set($capitalizedFilename = $NAME.substring(0,1).toUpperCase() + $NAME.substring(1))
const $capitalizedFilename = ({ $PROPS }) => (
  
);

export default $capitalizedFilename;
